/**
  * @desc services.js contains all the API URLs used in frontend to interact
  * with backend.
*/

const apiLocation = 'http://localhost:3000';

export default {
  getProductsList: `${apiLocation}/products`,
};
